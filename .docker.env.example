# Docker Environment Variables Example
# 
# This file contains example environment variables for Project Sentinel.
# Copy this file to `.docker.env` and replace all placeholder values with your actual configuration.
#
# IMPORTANT: Never commit `.docker.env` to version control - it contains sensitive information.
# This example file is safe to commit as it only contains placeholders.
#
# Usage:
#   1. Copy this file: cp .docker.env.example .docker.env
#   2. Edit .docker.env with your actual values
#   3. Run: docker compose --env-file .docker.env up -d

# Database Configuration
POSTGRES_DB=sentinal
POSTGRES_USER=sentinal_user
POSTGRES_PASSWORD=CHANGE_ME_GENERATE_SECURE_PASSWORD
POSTGRES_HOST=postgres
POSTGRES_PORT=5432

# Backend Configuration
FLASK_ENV=production
# Generate a secure random key: python -c "import secrets; print(secrets.token_urlsafe(32))"
SECRET_KEY=CHANGE_ME_GENERATE_SECURE_SECRET_KEY
# Generate a secure random key: python -c "import secrets; print(secrets.token_urlsafe(32))"
JWT_SECRET_KEY=CHANGE_ME_GENERATE_SECURE_JWT_SECRET_KEY
JWT_ACCESS_TOKEN_EXPIRE_MINUTES=30
JWT_REFRESH_TOKEN_EXPIRE_DAYS=7

# GitHub OAuth Configuration
# Get these from: https://github.com/settings/developers -> OAuth Apps
GITHUB_CLIENT_ID=your_github_client_id_here
GITHUB_CLIENT_SECRET=your_github_client_secret_here
GITHUB_CALLBACK_URL=http://localhost/callback

# Security Tools Configuration
TRIVY_API_URL=http://trivy:8080
OWASP_ZAP_API_URL=http://zap:8090
SONARQUBE_URL=http://sonarqube:9000
# Get SonarQube token from: SonarQube UI -> My Account -> Security -> Generate Token
SONARQUBE_TOKEN=your_sonarqube_token_here
SONARQUBE_PROJECT_KEY=sentinal

# Frontend Configuration
VITE_API_URL=http://localhost/api

# CORS Configuration
CORS_ORIGINS=http://localhost,http://localhost:80

# GDPR Compliance
GDPR_DATA_RETENTION_DAYS=365

# Logging
LOG_LEVEL=INFO

